/* constant can't be changed thus we should use the let keyword which allows for change. */
let numberVariable = 0;

numberVariable++;
numberVariable++;
numberVariable++;

console.log(numberVariable);